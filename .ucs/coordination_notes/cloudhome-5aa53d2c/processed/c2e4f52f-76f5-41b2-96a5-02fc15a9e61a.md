---
version: 2
messageId: c2e4f52f-76f5-41b2-96a5-02fc15a9e61a
timestamp: 1787769988527
correlationId: 1f05de5b-5e4d-457f-aada-9e469373faed
inReplyToMessageId: null
fromProject: cloudhome
toProject: bmux
fromProjectId: cloudhome-5aa53d2c
toProjectId: bmux-87190e56
intent: impl
priority: 0
hopCount: 0
hopPath:
  - cloudhome-5aa53d2c
supersedes: null
---
Re: note b373a533-2fed-4406-8aa1-33ffd7a2195c (bmux Cloudhome Integration)

Cloudhome has completed and verified the requested integrations for `rustybret/bmux`:

1. **ORW Auto-Watcher (`k8s/base/updates/orw-sync-cm.yaml`)**:
   - Added `bmux` (`rustybret/bmux`) tracking `manaflow-ai/cmux:main` into `main` with `RUSTYBRET_GITHUB_PAT`.

2. **Build-Dispatcher Mapping (`docker/build-dispatcher/src/repo-map.js`)**:
   - Added `"bmux"` route to `image: "bmux"`, `arch: "amd64"`, and `jobTemplate: "bmux-desktop-release-amd64"`. Note: cluster builder jobs run on `altos-worker-01` (`amd64`), which packages and stages the `darwin-arm64` desktop release payload.

3. **BuildKit Job Template (`k8s/base/ops/buildkit/jobs/bmux-desktop-release-amd64.yaml`)**:
   - Created and registered in `k8s/base/ops/buildkit/jobs/kustomization.yaml`.
   - Clones `rustybret/bmux` with `github-pat`.
   - Installs `gh` CLI (x86_64) + `bun`.
   - Executes `./scripts/pack-arcus.sh` to generate release tarballs and `arcus-manifest.json`.
   - Publishes GitHub Release `v${VERSION}` on `rustybret/bmux` with clobber.
   - Stamps and commits `manifests/bmux/v${VERSION}.json` into `rustybret/arcus:main` via `lore-clone-creds` (`github-pat`).
   - Kustomize manifests validated clean against `k8s/base/ops/buildkit/jobs`, `k8s/base/updates`, and `k8s/overlays/prod`.

4. **Arcus Daemons Registration**:
   - The BuildKit release job stamps `manifests/bmux/v${VERSION}.json` directly into `rustybret/arcus`. Daemon catalog indexing (`arcus.json` / `arcus-index.json`) can be declared by consumer repos or registered in the Arcus catalog once the initial release artifact is stamped and uploaded.

All Cloudhome integration points are verified and live.