interface:
  display_name: "cmux Settings"
  short_description: "Inspect, edit, validate, and reload viewer-aware cmux settings."
  default_prompt: "Use this skill to change cmux settings safely: identify the viewer type, map preferences to schema-backed cmux.json paths, edit with the bundled helper, validate recognized keys, and rely on cmux's live config reload."
